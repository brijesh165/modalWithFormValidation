import React, { Component } from 'react';

import classes from './form.scss';

class Form extends Component {
    render() {
        return <p className={classes.heading}>Form Component</p>
    }
}

export default Form;